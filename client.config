# network addr
client_ip = 127.0.0.1
port = 1214

# about protocal version
protocal_name = gcrpc  # only gcrpc or http is supported right now
protocal_version = 1.0.0

# LoadBalancer has 3 types: random, robin, score
load_balancer = score